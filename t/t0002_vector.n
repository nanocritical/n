from nlang.vector import *
from nlang import Zip

fun t1 = Bool
  let v = Vector.Ctor 100 1
  for x in v.Allidx
    if v[x] != 1
      return false
  for x in v.All
    if v != 1
      return false
  let w = Vector.Ctor v.Len
  for xv, xw in Zip v.All w.All
    xw = 

fun main = I32
  if t1
    return 0
  else:
    return 1
