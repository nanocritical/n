import nlang.meta
import nlang.literal
import nlang.unsafe
from nlang.numbers import *

(fun t) _sizeof x:t = size

(fun t) optional ptr:?@t otherwise:t = t
  if ptr != null
    let FIXME_cast:u32
    return ((nlang.unsafe.cast @t ?@t) ptr).
  else
    return otherwise
