-- See LICENSE for details. Originally derived from go/src/pkg/io
-- Copyright 2009 The Go Authors. All rights reserved.

from n import *

let EOF = {Msg=@"EOF"}:Error
let Short_write = {Msg=@"short write"}:Error
let Short_buffer = {Msg=@"short buffer"}:Error
let Unexpected_EOF = {Msg=@"unexpected EOF"}:Error

intf `Reader t:`Copyable =
	-- Returns a slice of b with the same origin, extended to include the
	-- content just read.
	met# Read b:[]!t = []!t, Error

alias `Writer = `Io_writer
