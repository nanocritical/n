from n import *

intf `Reader =
	-- Returns a slice of b with the content read.
	met# Read b:[]!U8 = Error, []!U8

intf `Writer =
	-- Returns a slice of b with the content not yet written.
	met# Write b:[]U8 = Error, []U8
