from nlang import *

fun foo = I32
  let x:?@I32
  such
    x = @1
  return x.

fun bar x:?@I32 a:Bool = I32
  let c
  such
    if a
      c = x !== null
    else
      c = false
  if c
    if a
      return x.

fun Main = I32
  return foo + (bar 1 true)
