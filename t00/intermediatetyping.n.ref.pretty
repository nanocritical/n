from nlang import *
fun noerror = i32 
  and x  such
    try
      and _Ngensym2 = { code=0 }
      x = 0
    catch _Ngensym1 _

      return 1
  return x

export fun main = i32 
  and x = 2 + (noerror)  such
    x = x + 1
    x = (max x 0) + 1
  return x - 4
