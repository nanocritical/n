from n import *

fun noerror = I32
	let x
	such
		try
			let except = Error.OK
			x = 0
		catch _
			return 1
	return x

fun Main = I32
	let x = 2 + noerror
	such
		x = x + 1
		x = (Max x 0) + 1
	return x - 4
